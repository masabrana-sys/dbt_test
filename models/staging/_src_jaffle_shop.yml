sources:
  - name: JAFFLE_SHOP_RAW
    database: STAGING
    schema: JAFFLE_SHOP_RAW
    tables:
      - name: RAW_ORDERS
        config: 
          freshness:
            warn_after:
              count: 12
              period: day
          loaded_at_field: ordered_at
      - name: RAW_CUSTOMERS
      - name: RAW_STORES
